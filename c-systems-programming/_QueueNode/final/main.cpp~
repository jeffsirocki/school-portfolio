// Jeff Sirocki
// CS2303 - A02

#include <iostream>
#include <string>

// Headers
#include "shopper.h"
#include "building_layout.h"

using namespace std;

// Main Function
int main ( int argc, char * argv[] ) {
  int i; // counters

  int numStores = atoi(argv[1]); // number of stores for shopper 1
  int time_slice = atoi(argv[2]); // time slice for RR scheduling

  // Shopper and store list
  Shopper S;
  int storeList[ numStores ][ 4 ];
  int xpos;
  int ypos;
  int zpos = 1; // always floor 2
  int service;

  // Prompt
  cout << "Enter in Shopper: " << endl;

  // Initialize Shopper

  // Initialize Building Layout


  // Simulate

  // end of main
  return 0;
}

  
